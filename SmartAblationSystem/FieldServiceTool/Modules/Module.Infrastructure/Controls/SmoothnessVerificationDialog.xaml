<UserControl x:Class="Module.Infrastructure.Controls.SmoothnessVerificationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Module.Infrastructure.Controls"
             xmlns:constants="clr-namespace:Module.Infrastructure.Constants"
             Width="1200" Height="600" Background="Transparent"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<prism:Dialog.WindowStyle>
		<Style TargetType="Window">
			<Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen"/>
			<Setter Property="ShowInTaskbar" Value="False"/>
			<Setter Property="WindowStyle" Value="None"/>
			<Setter Property="AllowsTransparency" Value="False"/>
			<Setter Property="Background" Value="#22B9B9B9"/>
			<Setter Property="Topmost" Value="True"/>
		</Style>
	</prism:Dialog.WindowStyle>
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="../Themes/PrismDialog.xaml"/>
				<ResourceDictionary Source="pack://application:,,,/Module.Infrastructure;component/Themes/Generic.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>
	<Border Style="{StaticResource PassFailBorderStyle}">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="45"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>
			<TextBlock Grid.Row="0" Text="{Binding Title}" Style="{StaticResource ConfirmationDialogTitleTextBlockStyle}"/>
			<Border Grid.Row="1" BorderThickness="1" CornerRadius="10" Background="Transparent">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="3*"/>
						<RowDefinition Height="*"/>
						<RowDefinition Height="55"/>
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>
					<Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Style="{StaticResource FlowMeterGraphBorderStyle}" Margin="12">
						<local:ChartControl x:Name="SmoothFMChart" Grid.Column="0" Margin="2"
						                    XAxisInterval="1" XMinorGridEnabled="True" Background="Black"
						                    YAxisInterval="500" SeriesName="flowSeries" DataPointsColor="LightGreen"/>
					</Border>
					<TextBlock Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" 
					           Text="{Binding Message}" Style="{StaticResource MessageTextBlockStyle}"/>
					<Grid Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2">
						<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
							<Button Style="{StaticResource ButtonOnDialogStylePass}" 
							        Content="{x:Static constants:Strings.PassText}"
							        Command="{Binding CloseDialogCommand}" 
							        CommandParameter="{x:Static constants:Strings.ParamYes}"/>
							<Button Style="{StaticResource ButtonOnDialogStyleFail}" 
							        Content="{x:Static constants:Strings.FailText}" 
							        Command="{Binding CloseDialogCommand}" 
							        CommandParameter="{x:Static constants:Strings.ParamNo}"/>
						</StackPanel>
					</Grid>
				</Grid>
			</Border>
		</Grid>
	</Border>
</UserControl>
