<UserControl x:Class="Module.Infrastructure.Controls.StateCheckFailureDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Width="760" Height="450"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
	<prism:Dialog.WindowStyle>
		<Style TargetType="Window">
			<Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen"/>
			<Setter Property="ShowInTaskbar" Value="False"/>
			<Setter Property="WindowStyle" Value="None"/>
			<Setter Property="AllowsTransparency" Value="True"/>
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="Topmost" Value="True"/>
		</Style>
	</prism:Dialog.WindowStyle>
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="../Themes/PrismDialog.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>
	<Border Style="{StaticResource StateCheckFailureMainBorderStyle}">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="45"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>
			<TextBlock Grid.Row="0" Text="{Binding Title}" Style="{StaticResource StateMessageTitleTextBlockStyle}"/>
			<Grid Grid.Row="1">
				<Grid.RowDefinitions>
					<RowDefinition Height="*"/>
					<RowDefinition Height="55"/>
				</Grid.RowDefinitions>
				<TextBox Grid.Row="0" Text="{Binding Message}" Style="{StaticResource StateMessageTextBoxStyle}"/>
				<Grid Grid.Column="0" Grid.Row="1">
					<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
						<Button Style="{StaticResource ButtonOnDialogStyle}" 
						        Content="{Binding ContinueText}" 
						        Command="{Binding CloseDialogCommand}" 
						        CommandParameter="{Binding ContinueParameter}"/>
						<Button Style="{StaticResource ButtonOnDialogStyle}" 
						        Content="{Binding RetryText}" 
						        Command="{Binding CloseDialogCommand}" 
						        CommandParameter="{Binding RetryParameter}"/>
						<Button Style="{StaticResource ButtonOnDialogStyle}" 
						        Content="{Binding StopText}" 
						        Command="{Binding CloseDialogCommand}" 
						        CommandParameter="{Binding StopParameter}"/>
					</StackPanel>
				</Grid>
			</Grid>
		</Grid>
	</Border>
</UserControl>
