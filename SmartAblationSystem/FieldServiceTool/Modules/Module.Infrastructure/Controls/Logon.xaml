<UserControl x:Class="Module.Infrastructure.Controls.Logon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
						 xmlns:prism="http://prismlibrary.com/"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800"
						 prism:ViewModelLocator.AutoWireViewModel="True"
             Width="700" Height="420" Background="Black" BorderBrush="{StaticResource AccentColorBlueSolidBrush}" BorderThickness="2">
	<UserControl.Effect>
		<DropShadowEffect Color="Wheat" Direction="0" BlurRadius="20" RenderingBias="Quality" Opacity="0.4"/>
	</UserControl.Effect>
	<prism:Dialog.WindowStyle>
		<Style TargetType="Window">
			<Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen"/>
			<Setter Property="ShowInTaskbar" Value="False"/>
			<Setter Property="WindowStyle" Value="None"/>
			<Setter Property="Topmost" Value="True"/>
			<Setter Property="AllowsTransparency" Value="True"/>
			<Setter Property="Background" Value="Transparent"/>
		</Style>
	</prism:Dialog.WindowStyle>
	<UserControl.Resources>
		<Style x:Key="TitleTextBlockStyle" TargetType="TextBlock">
			<Setter Property="HorizontalAlignment" Value="Center"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontSize" Value="30"/>
			<Setter Property="FontWeight" Value="Black"/>
		</Style>
		<Style x:Key="LogonTextBlockStyle" TargetType="TextBlock">
			<Setter Property="HorizontalAlignment" Value="Center"/>
			<Setter Property="VerticalAlignment" Value="Bottom"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontSize" Value="26"/>
			<Setter Property="FontWeight" Value="Heavy"/>
		</Style>
		<Style x:Key="NameTitleTextBlockStyle" TargetType="TextBlock">
			<Setter Property="HorizontalAlignment" Value="Right"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Margin" Value="0,0,20,0"/>
			<Setter Property="Foreground" Value="Gray"/>
			<Setter Property="FontSize" Value="22"/>
			<Setter Property="FontWeight" Value="Normal"/>
		</Style>
		<ControlTemplate x:Key="NameTextBoxControlTemplate" TargetType="TextBox">
			<Border x:Name="Bd" Background="#FF555555" BorderBrush="{StaticResource SystemMessageBrushBackground}" BorderThickness="3" CornerRadius="9">
				<ScrollViewer x:Name="PART_ContentHost"/>
			</Border>
			<ControlTemplate.Triggers>
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" TargetName="Bd"/>
					<Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
				</Trigger>
				<Trigger Property="Width" Value="Auto">
					<Setter Property="MinWidth" Value="100"/>
				</Trigger>
				<Trigger Property="Height" Value="Auto">
					<Setter Property="MinHeight" Value="50"/>
				</Trigger>
			</ControlTemplate.Triggers>
		</ControlTemplate>
		<Style x:Key="NameTextBoxStyle" TargetType="TextBox">
			<Setter Property="Template" Value="{StaticResource NameTextBoxControlTemplate}"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="HorizontalAlignment" Value="Left"/>
			<Setter Property="TextAlignment" Value="Center"/>
			<Setter Property="HorizontalContentAlignment" Value="Left"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="Padding" Value="10,0,0,0"/>
			<Setter Property="Margin" Value="10"/>
			<Setter Property="Width" Value="450"/>
			<Setter Property="Height" Value="40"/>
			<Setter Property="FontSize" Value="26"/>
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="MaxLength" Value="20"/>
		</Style>
	</UserControl.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="100"/>
			<RowDefinition Height="40"/>
			<RowDefinition Height="210"/>
			<RowDefinition Height="60"/>
		</Grid.RowDefinitions>
		<Image Grid.Row="0" Source="../Images/SmartFreeze.jpg" HorizontalAlignment="Left" VerticalAlignment="Top" Width="150" Margin="10"/>
		<Image Grid.Row="0" Source="../Images/BSC_Logo.png" HorizontalAlignment="Right" VerticalAlignment="Top" Width="70" Margin="0,10,10,0"/>
        <TextBlock Grid.Row="0" Text="{Binding ApplicationTitle}" Style="{StaticResource TitleTextBlockStyle}"/>
		<TextBlock Grid.Row="1" Text="Logon" Style="{StaticResource LogonTextBlockStyle}"/>
		<Grid Grid.Row="2" Margin="20">
			<Grid.RowDefinitions>
				<RowDefinition/>
				<RowDefinition/>
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="4*"/>
			</Grid.ColumnDefinitions>
			<TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding FirstNameTitle}" Style="{StaticResource NameTitleTextBlockStyle}"/>
			<TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" Style="{StaticResource NameTextBoxStyle}">
				<Validation.ErrorTemplate>
					<ControlTemplate>
						<StackPanel>
							<AdornedElementPlaceholder/>
							<ItemsControl ItemsSource="{Binding}">
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<TextBlock Text="{Binding ErrorContent}" Foreground="white"/>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</StackPanel>
					</ControlTemplate>
				</Validation.ErrorTemplate>
			</TextBox>
			<TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding LastNameTitle}" Style="{StaticResource NameTitleTextBlockStyle}"/>
			<TextBox Grid.Row="1" Grid.Column="1" Text="{Binding LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" Style="{StaticResource NameTextBoxStyle}">
				<Validation.ErrorTemplate>
					<ControlTemplate>
						<StackPanel>
							<AdornedElementPlaceholder/>
							<ItemsControl ItemsSource="{Binding}">
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<TextBlock Text="{Binding ErrorContent}" Foreground="White"/>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</StackPanel>
					</ControlTemplate>
				</Validation.ErrorTemplate>
			</TextBox>
		</Grid>
		<Grid Grid.Row="3">
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>
			<Button Grid.Column="0" Command="{Binding CloseLogonCommand}" CommandParameter="{Binding OkButtonCommandParameter}" 
			        Style="{StaticResource ButtonOnDialogStyle}" Content="{Binding OkText}" HorizontalAlignment="Right"/>
			<Button Grid.Column="1" Command="{Binding QuitLogonCommand}" CommandParameter="{Binding QuitButtonCommandParameter}" 
			        Style="{StaticResource ButtonOnDialogStyle}" Content="{Binding QuitText}" HorizontalAlignment="Left"/>
			
		</Grid>
	</Grid>
</UserControl>
