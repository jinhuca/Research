<UserControl
    x:Class="SmartAblationSystem.SplashScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    mc:Ignorable="d"
    d:DataContext="{x:Static dx:SplashScreenViewModel.DesignTimeData}"
    >
    <UserControl.Resources>
		<SolidColorBrush x:Key="CryterionBackgroundBrush" Color="#FF009ED6" />
	</UserControl.Resources>
    <Grid x:Name="LayoutRoot">
        <Grid x:Name="Splash" Width="1024" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0">
            <Grid x:Name="Back">
                <Border Background="Black" CornerRadius="3" Opacity="1"/>
                <Border CornerRadius="2" Margin="1" Background="Black"/>
            </Grid>
            <Grid x:Name="Content_Area" Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Image x:Name="Image" Source="Smartfreeze - large.png" Stretch="None"/>
                <!-- TextBlock x:Name="Info" TextWrapping="Wrap" Text="{Binding State}" Grid.Row="1" Margin="12,12,12,0" Foreground="#FF2D2D2D"-->
				<Controls:ProgressRing Height="60" Width="60" MinHeight="60" MinWidth="60" Padding="10" Foreground="{StaticResource CryterionBackgroundBrush}"
                                        IsEnabled="True" Margin="491,400,459,93" IsLarge="True" />
                <Label x:Name="CanErrorLabel" Uid="VerifyCanBusHardwareConnectionsUID" Content="{Binding RelativeSource={RelativeSource Self}, Path=Uid}" Margin="320,550,230,93" Foreground="Red" Width="450" FontSize="20" FontWeight="Bold"  HorizontalAlignment="Center"   />

                <!-- DockPanel x:Name="Footer" Grid.Row="3" Margin="12,20,12,4">
                    <TextBlock x:Name="Footer_Text" TextWrapping="Wrap" Opacity="0.5" Foreground="#FF2D2D2D" HorizontalAlignment="Left" VerticalAlignment="Center"
                               Text="{Binding Source={x:Static sys:DateTime.Today}, Path=Year, StringFormat='{}Copyright © 2017-{0}'}"/>
                    <Image x:Name="Logotype" DockPanel.Dock="Right" Source="Logo.png" Stretch="None" HorizontalAlignment="Right"  />
                <-->
            </Grid>
        </Grid>
    </Grid>
</UserControl>
