<Window x:Class="SmartAblationSystem.Views.DeleteUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Height="203" Width="507">
  <Border CornerRadius="12" Height="203" Width="507" Opacity="1" 
          BorderBrush="Transparent" 
          BorderThickness="0" 
          Background="{StaticResource PanelBackgroundBrush_}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="30"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <Border Grid.Row="0" 
              CornerRadius="12,12,0,0" 
              Background="{StaticResource TitleBackgroundBrush_}"/>
      <Border Grid.Row="1" 
              CornerRadius="0,0,12,12" 
              BorderBrush="Transparent" 
              BorderThickness="0" 
              Background="{StaticResource BorderBackgroundBrush_}">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <TextBlock Grid.Row="0" 
                     Text="{Binding DeleteUserMessage}" 
                     Style="{StaticResource DeleteUserTextBlockStyle_}"/>
          <Grid Grid.Row="1" 
                Margin="0,0,18,0" 
                HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
              <ColumnDefinition/>
              <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" 
                    Content="Cancel" 
                    Style="{StaticResource TextWithUnderlineButton_}" 
                    Click="Cancel_Click"
                    PreviewTouchDown="CancelButton_PreviewTouchDown"
                    IsCancel="True"/>
            <Button Grid.Column="1" 
                    Content="CONFIRM" 
                    Foreground="White" 
                    Style="{StaticResource ConfirmButtonStyle_}" 
                    Click="Confirm_Click"
                    PreviewTouchDown="ConfirmButton_PreviewTouchDown"/>
          </Grid>
        </Grid>
      </Border>
    </Grid>
  </Border>
</Window>
