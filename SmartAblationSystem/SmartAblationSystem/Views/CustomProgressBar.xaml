<Window x:Class="SmartAblationSystem.Views.CustomProgressBar"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:SmartAblationSystem.Views"
      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
      xmlns:converters="clr-namespace:SmartAblationSystem.Converters"
      mc:Ignorable="d"
      d:DesignHeight="300" d:DesignWidth="300"
      Title="CustomCustomProgressBar"  Height="240" Width="300" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Window.Resources>
        <converters:FieldToTextConverter x:Key="fieldToTextConverter" />
    </Window.Resources>


    <Border Name="BorderColor" BorderThickness="0" CornerRadius="10,10,10,10" Background="{StaticResource CryterionPopupSystemMessageBrushBackground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Label Name="TitleLabel" Grid.Row="0" Uid="pleasewaitTransUID" Content="{Binding RelativeSource={RelativeSource Self}, Path=Uid, Converter={StaticResource fieldToTextConverter}}" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White" Background="Transparent" />

            <Border Grid.Row="1" BorderThickness="1" CornerRadius="10" Background="{StaticResource CryterionPopupSystemMessageBrushControlContainer}" Margin="5,5,5,5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" BorderThickness="1" CornerRadius="10" Background="{StaticResource CryterionPopupSystemMessageBrushContentContainer }" Margin="5,5,5,5">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Viewbox VerticalAlignment="Top" Height="142" Margin="0,10,0,0" Grid.RowSpan="2">
                                <!--Controls:MetroProgressBar  IsIndeterminate="True" Height="90" Width="297"-->
                                <Controls:ProgressRing Foreground="{StaticResource CryterionPopupSystemMessageBrushBackground}" ></Controls:ProgressRing>
                            </Viewbox>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>