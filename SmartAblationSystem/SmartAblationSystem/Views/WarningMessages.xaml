<Window x:Class="SmartAblationSystem.Views.WarningMessages"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SmartAblationSystem.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:converters="clr-namespace:SmartAblationSystem.Converters"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             SizeToContent="WidthAndHeight" 
             WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="Transparent" 
             WindowStyle="None" AllowsTransparency="True">
  <Window.Resources>
    <converters:BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter" />
    <converters:BooleanToNotVisibilityConverter x:Key="booleanToNotVisibilityConverter" />
    <converters:CrtAndDoctorMessageConverter x:Key="crtAndDoctorMessageConverter" />

    <Color x:Key="SelectedRowBackgroundColor">#ff3d4968</Color>
    <SolidColorBrush x:Key="SelectedRowBackgroundBrush" Color="{StaticResource SelectedRowBackgroundColor}" />

    <Style TargetType="TextBlock" x:Key="MessageBaseStyle">
      <Setter Property="FontFamily" Value="{StaticResource Lato}" />
      <Setter Property="FontSize" Value="18" />
      <Setter Property="FontWeight" Value="Regular" />
      <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="CaptionHeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource MessageBaseStyle}">
      <Setter Property="FontSize" Value="20"/>
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="TextAlignment" Value="Center" />
      <Setter Property="Foreground" Value="#FFFFFFFF" />
    </Style>

    <Style TargetType="ScrollBar">
      <Setter Property="Width" Value="15"/>
      <Setter Property="Padding" Value="3"/>
      <Setter Property="Background" Value="#383843"/>
      <Setter Property="BorderBrush" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ScrollBar">
            <Grid x:Name="GridRoot" Width="{TemplateBinding Width}" Background="Transparent">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4"/>
              </Grid.ColumnDefinitions>
              <Grid.RowDefinitions>
                <RowDefinition Height="5"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="5"/>
              </Grid.RowDefinitions>
              <Border Grid.RowSpan="3" Grid.ColumnSpan="3" CornerRadius="7" Grid.ZIndex="-1" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"/>
              <Track Name="PART_Track" 
                               ViewportSize="NaN"
                               IsDirectionReversed="true"
                               Grid.Row="1"
                               Grid.Column="1"
                               Grid.ZIndex="-1">
                <Track.Thumb>
                  <Thumb Style="{StaticResource sf.Scrollbar.Thumb}"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton x:Name="PageUp" 
                                          Style="{StaticResource RepeatButtonPageStyle}" 
                                          Command="ScrollBar.PageDownCommand"/>
                </Track.IncreaseRepeatButton>
                <Track.DecreaseRepeatButton>
                  <RepeatButton x:Name="PageDown" 
                                          Style="{StaticResource RepeatButtonPageStyle}" 
                                          Command="ScrollBar.PageUpCommand"/>
                </Track.DecreaseRepeatButton>
              </Track>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="LayoutTransform" TargetName="GridRoot">
                  <Setter.Value>
                    <RotateTransform Angle="-90"/>
                  </Setter.Value>
                </Setter>
                <Setter TargetName="PART_Track" Property="Orientation" Value="Vertical" />
                <Setter Property="Command" Value="ScrollBar.PageLeftCommand" TargetName="PageDown" />
                <Setter Property="Command" Value="ScrollBar.PageRightCommand" TargetName="PageUp" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="DataGridRow">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="VerticalAlignment" Value="Center" />
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{StaticResource SelectedRowBackgroundBrush}"/>
          <Setter Property="BorderBrush" Value="{x:Null}"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style TargetType="DataGridCell">
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="FontFamily" Value="{StaticResource Lato}"/>
      <Setter Property="FontSize" Value="16"/>
      <Setter Property="FontWeight" Value="Regular"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="HorizontalAlignment" Value="Left"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
      <Setter Property="Height" Value="30"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type DataGridCell}">
            <Grid Background="{TemplateBinding Background}">
              <ContentPresenter VerticalAlignment="{TemplateBinding VerticalAlignment}" HorizontalAlignment="{TemplateBinding HorizontalAlignment}"/>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{x:Null}"/>
          <Setter Property="BorderBrush" Value="{x:Null}"/>
        </Trigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>

  <Border Width="862" Height="580" Background="#ff2b2b33" CornerRadius="8" BorderThickness="0" Margin="0,0,10,10">
    <Border.Effect>
      <DropShadowEffect BlurRadius="15" Opacity="0.8" Color="#ff2b2b33" ShadowDepth="5"/>
    </Border.Effect>
    <Grid Margin="0" >
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Grid Grid.Row="0">
        <Path Width="862" Height="78" Data="M 10,0 L 852,0 C 857.5,0 862,4.5 862,10 L 862,78 L 0,78 L 0,10 C 0,4.5 4.5,0 10,0 Z" 
              Fill="#ffc12b48" Stroke="#ff707070" StrokeThickness="0" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
          <TextBlock Text="SYSTEM MESSAGE" Style="{StaticResource CaptionHeaderTextStyle}" />
        </StackPanel>
      </Grid>

      <Grid Grid.Row="1" VerticalAlignment="Center" Height="380" Width="781" Margin="40, 20, 40, 0">
        <DataGrid Background="#FF34343E"
                  HeadersVisibility="None"
                  HorizontalScrollBarVisibility="Disabled"
                  BorderThickness="0"
                  CanUserAddRows="False"
                  AutoGenerateColumns="False"
                  EnableColumnVirtualization="True"  
                  EnableRowVirtualization="True" 
                  VerticalScrollBarVisibility="Auto"
                  KeyboardNavigation.TabNavigation="Once"
                  IsReadOnly="False" 
                  SelectionMode="Extended"  
                  SelectionUnit="FullRow"  
                  VirtualizingPanel.VirtualizationMode="Recycling" 
                  GridLinesVisibility="None"
                  ItemsSource="{Binding WarningMessagesList}">
          <DataGrid.Columns>
            <DataGridTextColumn Width="*" MinWidth="100" CanUserResize="False" IsReadOnly="True" >
              <DataGridTextColumn.Binding>
                <MultiBinding Converter="{StaticResource crtAndDoctorMessageConverter}" >
                  <Binding Path="CrtMessage" />
                  <Binding Path="Message" />
                </MultiBinding>
              </DataGridTextColumn.Binding>
            </DataGridTextColumn>
          </DataGrid.Columns>
        </DataGrid>
      </Grid>

      <Grid Grid.Row="2" Height="90" >
        <Grid.ColumnDefinitions>
          <ColumnDefinition />
          <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" >
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <Button Grid.Column="0" Content="Close" Style="{StaticResource TextWithUnderlineButton}" Width="100" Height="48" HorizontalAlignment="Right" 
                  IsCancel="True" IsDefault="True"/>
          <Grid Grid.Column="1" Width="180">
            <Button  Content="RESET SYSTEM" Style="{StaticResource SettingsPreferencesButton}" Width="155" Height="48" HorizontalAlignment="Right" 
                     Visibility="{Binding IsClearListVisible, Converter={StaticResource booleanToVisibilityConverter}}"
                   Command="{Binding ClearMessagesCommand}"/>
            <Controls:ProgressRing Visibility="{Binding IsClearListVisible, Converter={StaticResource booleanToNotVisibilityConverter}}" 
                                   Height="50" Width="50" Foreground="{StaticResource SelectedRowBackgroundBrush}" />
          </Grid>
        </Grid>
      </Grid>
    </Grid>

  </Border>

</Window>