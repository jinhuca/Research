<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style x:Key="ContentTextStyle" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource Lato}"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="FontSize" Value="16"/>
    <Setter Property="Foreground" Value="White"/>
    <Setter Property="HorizontalAlignment" Value="Center"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
  </Style>

  <ControlTemplate x:Key="SaveButtonTemplate_" TargetType="Button">
    <ControlTemplate.Resources>
      <Storyboard x:Key="Pressed_">
        <DoubleAnimation Storyboard.TargetName="grid_"
                         Storyboard.TargetProperty="Opacity"
                         To="0.2"
                         Duration="0:0:0.2"/>
      </Storyboard>
      <Storyboard x:Key="Disabled_">
        <DoubleAnimation Storyboard.TargetName="grid_"
                         Storyboard.TargetProperty="Opacity"
                         To="0.2"
                         Duration="0:0:0"/>
      </Storyboard>
    </ControlTemplate.Resources>
    <Grid x:Name="grid_" 
          Width="{TemplateBinding Width}" 
          Height="{TemplateBinding Height}"
          HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
          VerticalAlignment="{TemplateBinding VerticalAlignment}"
          Background="Transparent">
      <VisualStateManager.VisualStateGroups>
        <VisualStateGroup x:Name="CommonStates">
          <VisualState x:Name="Normal"/>
          <VisualState x:Name="MouseOver"/>
          <VisualState x:Name="Pressed" Storyboard="{StaticResource Pressed_}"/>
          <VisualState x:Name="Disabled" Storyboard="{StaticResource Disabled_}"/>
        </VisualStateGroup>
      </VisualStateManager.VisualStateGroups>
      <Path Fill="{TemplateBinding Background}" 
            Stroke="#ff4a5f8f" 
            StrokeThickness="2"
            Data="M24.5,0 L59.600006,0 C73.100006,0 84.100006,11 84.100006,24.5 84.100006,38 73.100006,49 59.600006,49 
						L24.5,49 C11,49 0,38 0,24.5 0,11 11,0 24.5,0 z"/>
      <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
			                  VerticalAlignment="{TemplateBinding VerticalAlignment}"
			                  Margin="{TemplateBinding Padding}"
			                  Focusable="False"
			                  RecognizesAccessKey="True"/>
    </Grid>
  </ControlTemplate>

  <Style x:Key="SaveButtonStyle_" TargetType="Button" x:Shared="False">
    <Setter Property="Template" Value="{StaticResource SaveButtonTemplate_}"/>
    <Setter Property="Width" Value="86"/>
    <Setter Property="Height" Value="50"/>
    <Setter Property="Background" Value="#ff38425e"/>
    <Setter Property="HorizontalAlignment" Value="Center"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
    <Setter Property="Content">
      <Setter.Value>
        <TextBlock Style="{StaticResource ContentTextStyle}"
				           Text="SAVE"/>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>