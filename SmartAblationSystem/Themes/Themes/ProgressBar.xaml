<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:CustomControls.Converters">
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="Shared.xaml"/>
  </ResourceDictionary.MergedDictionaries>

  <Color x:Key="ControlMediumColor">#ff7bc1f1</Color>
  <Color x:Key="ControlDarkColor">#ff29526e</Color>
  <Color x:Key="WarningColor">#FFFFE472</Color>
  <Color x:Key="WarningColorDark">#FFD1A146</Color>
  <Color x:Key="FailColor">#FFF21919</Color>
  <Color x:Key="FailColorDark">#FF6C0A08</Color>

  <LinearGradientBrush x:Key="ProgressBarIndicatorAnimatedFill"
	                     StartPoint="0,0"
	                     EndPoint="1,0">
    <LinearGradientBrush.GradientStops>
      <GradientStopCollection>
        <GradientStop Color="#337bc1f1" Offset="0"/>
        <GradientStop Color="#337bc1f1" Offset="1"/>
      </GradientStopCollection>
    </LinearGradientBrush.GradientStops>
  </LinearGradientBrush>

  <LinearGradientBrush StartPoint="0,0" EndPoint="1,0" x:Key="DefaultTankColorBrush">
    <GradientStop Color="{DynamicResource ControlMediumColor}" Offset="1"/>
    <GradientStop Color="{DynamicResource ControlDarkColor}" Offset="0"/>
  </LinearGradientBrush>

  <LinearGradientBrush StartPoint="0,0" EndPoint="1,0" x:Key="TankLowWarningColorBrush">
    <GradientStop Color="{DynamicResource WarningColor}" Offset="1"/>
    <GradientStop Color="{DynamicResource WarningColorDark}" Offset="0"/>
  </LinearGradientBrush>
  <LinearGradientBrush StartPoint="0,0" EndPoint="1,0" x:Key="TankTooLowFailureColorBrush">
    <GradientStop Color="{DynamicResource FailColor}" Offset="1"/>
    <GradientStop Color="{DynamicResource FailColorDark}" Offset="0"/>
  </LinearGradientBrush>
  <Color x:Key="ControlLightColor">White</Color>

  <Style x:Key="TankFillMeterStyle"
	       TargetType="ProgressBar">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ProgressBar">
          <ControlTemplate.Resources>
            <converters:IsValueGreaterThanConverter x:Key="isValueGreaterThanConverter" />
          </ControlTemplate.Resources>
          <Grid MinHeight="14" 
                x:Name="TemplateRoot" 
                MinWidth="{TemplateBinding Width}" 
                Background="{TemplateBinding Background}">
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Determinate"/>
                <VisualState x:Name="Indeterminate">
                  <Storyboard>
                    <ObjectAnimationUsingKeyFrames Duration="00:00:00" 
									                                 Storyboard.TargetName="PART_Indicator" 
									                                 Storyboard.TargetProperty="Background">
                      <DiscreteObjectKeyFrame KeyTime="00:00:00">
                        <DiscreteObjectKeyFrame.Value>
                          <SolidColorBrush>Transparent</SolidColorBrush>
                        </DiscreteObjectKeyFrame.Value>
                      </DiscreteObjectKeyFrame>
                    </ObjectAnimationUsingKeyFrames>
                  </Storyboard>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <Grid HorizontalAlignment="Stretch" 
						      VerticalAlignment="Stretch" 
						      Margin="1,3,1,3" 
						      ClipToBounds="True" 
						      Background="Transparent">
              <Border x:Name="PART_Indicator" 
                      CornerRadius="16,2,2,16" 
                      BorderThickness="0" 
                      HorizontalAlignment="Left" 
                      Background="{TemplateBinding Foreground}" 
                      ClipToBounds="True">
                <Grid ClipToBounds="True" 
								      x:Name="Animation">
                  <Rectangle x:Name="PART_GlowRect" 
									           Width="100" 
									           HorizontalAlignment="Left" 
									           Fill="{StaticResource ProgressBarIndicatorAnimatedFill}" 
									           Margin="0,0,0,0"/>
                </Grid>
              </Border>
            </Grid>

            <Border BorderBrush="Transparent" 
						        CornerRadius="16" 
						        BorderThickness="1">
              <Border x:Name="PART_Track"
							        CornerRadius="9"
							        BorderThickness="1.5" 
							        BorderBrush="#ffffffff"
							        Background="Transparent"
							        ClipToBounds="True"/>
            </Border>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
              <Setter Property="LayoutTransform" TargetName="TemplateRoot">
                <Setter.Value>
                  <TransformGroup>
                    <RotateTransform Angle="-90"/>
                  </TransformGroup>
                </Setter.Value>
              </Setter>
              <Setter Property="LayoutTransform" TargetName="PART_Indicator">
                <Setter.Value>
                  <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
              </Setter>
            </Trigger>

            <DataTrigger Binding="{Binding Path=Value, RelativeSource={RelativeSource self},
              Converter={StaticResource isValueGreaterThanConverter}, ConverterParameter=93}" Value="True">
              <Setter TargetName="PART_Indicator" Property="CornerRadius" Value="16, 6, 6, 16"/>
            </DataTrigger>

          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Foreground" Value="{StaticResource DefaultTankColorBrush}"/>
    <Style.Triggers>
      <DataTrigger Binding="{Binding GasState}" Value="0">
        <Setter Property="Foreground" Value="{StaticResource TankLowWarningColorBrush}"/>
      </DataTrigger>
      <DataTrigger Binding="{Binding GasState}" Value="1">
        <Setter Property="Foreground" Value="{StaticResource TankTooLowFailureColorBrush}"/>

      </DataTrigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="TankValueTextBlockStyle" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource Lato}"/>
    <Setter Property="FontWeight" Value="Medium"/>
    <Setter Property="FontSize" Value="28"/>
    <Setter Property="Foreground" Value="#ffffffff"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
  </Style>

  <Style x:Key="TankDetailsTextBlockStyle" TargetType="TextBlock">
    <Setter Property="Foreground" Value="#ffcdc2c5"/>
    <Setter Property="FontFamily" Value="{StaticResource Lato}"/>
    <Setter Property="FontWeight" Value="Medium"/>
    <Setter Property="FontSize" Value="16"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
  </Style>

  <Style x:Key="HorizontalProgressBar_" TargetType="ProgressBar">
    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Foreground" Value="White"/>
    <Setter Property="FontSize" Value="30"/>
    <Setter Property="Padding" Value="5,0"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
    <Setter Property="Margin" Value="15,0,0,0"/>
    <Setter Property="Width" Value="240"/>
    <Setter Property="Height" Value="20"/>
    <Setter Property="Background">
      <Setter.Value>
        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
          <GradientStop Color="#4b6090" Offset="0"/>
          <GradientStop Color="#2d3b5a" Offset="1"/>
        </LinearGradientBrush>
      </Setter.Value>
    </Setter>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ProgressBar">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Height="{TemplateBinding Height}">
              <Grid>
                <Border x:Name="PART_Track" Background="#ff1D1D26" CornerRadius="14">
                  <Grid x:Name="PART_Indicator" ClipToBounds="True" HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition x:Name="width1"/>
                      <ColumnDefinition x:Name="width2" Width="0"/>
                    </Grid.ColumnDefinitions>
                    <Grid x:Name="Animation" RenderTransformOrigin="0.5,0.5">
                      <Grid.RenderTransform>
                        <TransformGroup>
                          <ScaleTransform ScaleY="-1" ScaleX="1"/>
                          <SkewTransform AngleY="0" AngleX="0"/>
                          <RotateTransform Angle="180"/>
                          <TranslateTransform/>
                        </TransformGroup>
                      </Grid.RenderTransform>
                      <Border Background="{TemplateBinding Background}" CornerRadius="14" Margin="1">
                        <Viewbox HorizontalAlignment="Left" StretchDirection="DownOnly" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="True"/>
                      </Border>
                    </Grid>
                  </Grid>
                </Border>
              </Grid>
            </Grid>
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Determinate"/>
                <VisualState x:Name="Indeterminate">
                  <Storyboard RepeatBehavior="Forever">
                    <PointAnimationUsingKeyFrames Storyboard.TargetName="Animation" Storyboard.TargetProperty="(UIElement.RenderTransformOrigin)">
                      <EasingPointKeyFrame KeyTime="0:0:0" Value="0.5,0.5"/>
                      <EasingPointKeyFrame KeyTime="0:0:1.5" Value="1.95,0.5"/>
                      <EasingPointKeyFrame KeyTime="0:0:3" Value="0.5,0.5"/>
                    </PointAnimationUsingKeyFrames>
                  </Storyboard>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Background" Value="#c5c5c5"/>
            </Trigger>
            <Trigger Property="IsIndeterminate" Value="true">
              <Setter TargetName="width1" Property="Width" Value="0.25*"/>
              <Setter TargetName="width2" Property="Width" Value="0.725*"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="VerticalProgressBar_" TargetType="ProgressBar" BasedOn="{StaticResource HorizontalProgressBar_}">
    <Setter Property="Foreground" Value="White"/>
    <Setter Property="Background" Value="{StaticResource AccentColorBlueSolidBrush}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ProgressBar" >
          <Grid x:Name="Root">
            <Border Name="PART_Track" Background="DarkGray" CornerRadius="8"/>
            <Grid x:Name="PART_Indicator" ClipToBounds="True" HorizontalAlignment="Left">
              <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="width1"/>
                <ColumnDefinition x:Name="width2" Width="0"/>
              </Grid.ColumnDefinitions>
              <Grid x:Name="Animation" RenderTransformOrigin="0.5,0.5">
                <Grid.RenderTransform>
                  <TransformGroup>
                    <ScaleTransform ScaleY="-1" ScaleX="1"/>
                    <SkewTransform AngleY="0" AngleX="0"/>
                    <RotateTransform Angle="180"/>
                    <TranslateTransform/>
                  </TransformGroup>
                </Grid.RenderTransform>
                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                  <Viewbox HorizontalAlignment="Left" StretchDirection="DownOnly" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                  </Viewbox>
                </Border>
              </Grid>
            </Grid>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
              <Setter TargetName="Root" Property="LayoutTransform">
                <Setter.Value>
                  <RotateTransform Angle="270"/>
                </Setter.Value>
              </Setter>
              <Setter TargetName="Root" Property="Width" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Height}"/>
              <Setter TargetName="Root" Property="Height" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Width}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>