<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="Shared.xaml"/>
  </ResourceDictionary.MergedDictionaries>

  <SolidColorBrush x:Key="PasswordErrorForegroundBrush_" Color="#FFFB5B5B"/>

  <ControlTemplate x:Key="PasswordBoxTemplate" TargetType="{x:Type PasswordBox}">
    <ControlTemplate.Resources>
      <Storyboard x:Key="Hovering">
        <ColorAnimation Storyboard.TargetName="theBorderBrush"
                       Storyboard.TargetProperty="Color"
                       To="{DynamicResource Color_Border_MouseOver}"
                       Duration="0"/>
        <ColorAnimation Storyboard.TargetName="theBorderBackground"
                       Storyboard.TargetProperty="Color"
                       To="{DynamicResource Color_Background_MouseOver}"
                       Duration="0"/>
      </Storyboard>
      <Storyboard x:Key="Focusing">
        <ColorAnimation Storyboard.TargetName="theBorderBrush"
                       Storyboard.TargetProperty="Color"
                       To="{DynamicResource Color_Border_Focused}"
                       Duration="0"/>
        <ColorAnimation Storyboard.TargetName="theBorderBackground"
                       Storyboard.TargetProperty="Color"
                       To="{DynamicResource Color_Background_Focused}"
                       Duration="0"/>
      </Storyboard>
    </ControlTemplate.Resources>

    <Border x:Name="Border" Opacity="1" CornerRadius="5" BorderThickness="1"
            Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}">
      <!--<Border.BorderBrush>
        <SolidColorBrush x:Name="theBorderBrush" Color="{DynamicResource Color_Border_Normal}"/>
      </Border.BorderBrush>
      <Border.Background>
        <SolidColorBrush x:Name="theBorderBackground" Color="{DynamicResource Color_Background_Normal}"/>
      </Border.Background>-->
      <VisualStateManager.VisualStateGroups>
        <!--<VisualStateGroup x:Name="CommonStates">
          <VisualState x:Name="Normal"/>
          <VisualState x:Name="MouseOver" Storyboard="{StaticResource Hovering}"/>
          <VisualState x:Name="Disabled"/>
          <VisualState x:Name="ReadOnly"/>
        </VisualStateGroup>
        <VisualStateGroup x:Name="FocusStates">
          <VisualState x:Name="Focused" Storyboard="{StaticResource Focusing}"/>
          <VisualState x:Name="Unfocused"/>
        </VisualStateGroup>
        <VisualStateGroup x:Name="ValidationStates">
          <VisualState x:Name="Valid"/>
          <VisualState x:Name="InvalidFocused"/>
          <VisualState x:Name="InvalidUnfocused"/>
        </VisualStateGroup>-->
      </VisualStateManager.VisualStateGroups>
      <ScrollViewer Margin="10,0,0,0" x:Name="PART_ContentHost" Style="{DynamicResource ScrollViewer}"/>
    </Border>
  </ControlTemplate>

  <Style x:Key="PasswordBoxStyle_" TargetType="PasswordBox">
    <Setter Property="Template" Value="{StaticResource PasswordBoxTemplate}"/>
    <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    <Setter Property="AllowDrop" Value="true"/>
    <Setter Property="Height" Value="33"/>
    <Setter Property="FontFamily" Value="{StaticResource Lato}"/>
    <Setter Property="FontWeight" Value="Regular"/>
    <Setter Property="FontSize" Value="18"/>
    <Setter Property="HorizontalContentAlignment" Value="Left"/>
    <Setter Property="HorizontalAlignment" Value="Stretch"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="VerticalAlignment" Value="Top"/>
    <Setter Property="Foreground" Value="#ffffffff"/>
    <Setter Property="Background" Value="#ff26262e"/>
    <Setter Property="Validation.ErrorTemplate">
      <Setter.Value>
        <ControlTemplate>
          <StackPanel>
            <AdornedElementPlaceholder/>
            <ItemsControl ItemsSource="{Binding}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding ErrorContent}"
                             HorizontalAlignment="Left"
                             Margin="0,3,0,0"
                             Foreground="{StaticResource PasswordErrorForegroundBrush_}" 
                             FontSize="14"/>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="Validation.HasError" Value="true">
        <Setter Property="BorderBrush" Value="#888E4646"/>
      </Trigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>