<UserControl x:Class="CustomControls.UserControls.ETSCircleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:CustomControls.Behaviors"
             mc:Ignorable="d">
  <UserControl.Resources>
    <Style x:Key="etsTempTextStyle" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Arial"/>
      <Setter Property="FontSize" Value="60" />
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="TextAlignment" Value="Center" />
      <Setter Property="Foreground" Value="#ffffffff" />
      <Style.Triggers>
        <DataTrigger 
          Binding="{Binding IsAlert, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" Value="True">
          <Setter Property="Foreground" Value="#ff2b2b33" />
        </DataTrigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="etsConfigTempTextStyle" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Arial"/>
      <Setter Property="FontSize" Value="19" />
      <Setter Property="FontWeight" Value="DemiBold" />
      <Setter Property="HorizontalAlignment" Value="Center" />
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="Foreground" Value="#ffff9d57" />
      <Style.Triggers>
        <DataTrigger 
          Binding="{Binding IsAlert, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" Value="True">
          <Setter Property="Foreground" Value="#ff2b2b33" />
        </DataTrigger>
      </Style.Triggers>
    </Style>

    <Style TargetType="Path">
      <Setter Property="Stretch" Value="Uniform" />
      <Setter Property="Fill" Value="#ff464650" />
      <Setter Property="Stroke" Value="#ff707070"/>
      <Setter Property="StrokeThickness" Value="0.0"/>
      <Setter Property="Effect">
        <Setter.Value>
          <DropShadowEffect Opacity="0.5" BlurRadius="10" Direction="135"/>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <DataTrigger 
          Binding="{Binding IsAlert, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" Value="True">
          <Setter Property="Fill" Value="#fffb842d" />
        </DataTrigger>
      </Style.Triggers>
    </Style>

    <Storyboard x:Key="FlashStoryboard" RepeatBehavior="Forever">
      <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" From="1" To="0.6" 
                       Duration="00:00:1.5" RepeatBehavior="Forever" AutoReverse="True"/>
    </Storyboard>

    <Style TargetType="Grid" x:Key="FlashStyle">
      <Style.Triggers>
        <DataTrigger Binding="{Binding IsAlert, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" Value="True">
          <DataTrigger.EnterActions>
            <BeginStoryboard Name="_flashStoryboard" Storyboard="{StaticResource FlashStoryboard}"/>
          </DataTrigger.EnterActions>
          <DataTrigger.ExitActions>
            <StopStoryboard BeginStoryboardName="_flashStoryboard" />
          </DataTrigger.ExitActions>
        </DataTrigger>
      </Style.Triggers>
    </Style>
  </UserControl.Resources>

  <Grid Width="150" Height="150" Name="_mainGrid" Style="{StaticResource FlashStyle}">
    <b:Interaction.Behaviors>
      <behaviors:StoryboardSyncBehavior SyncGroupName="AlertGroup"/>
    </b:Interaction.Behaviors>
    <Grid.RowDefinitions>
      <RowDefinition Height="40"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="40"/>
    </Grid.RowDefinitions>
    <Path Grid.RowSpan="3"  Width="150" Height="150" Stretch="uniform" Data="
               M 75 0 
               C 116.4213409423828 0 150 33.57864379882812 150 75 
               C 150 116.4213409423828 116.4213409423828 150 75 150 
               C 33.57864379882812 150 0 116.4213409423828 0 75 
               C 0 33.57864379882812 33.57864379882812 0 75 0 
               Z" 
               Canvas.Left="0" Canvas.Top="0"/>
    <TextBlock Grid.Row="1" Style="{StaticResource etsTempTextStyle}" 
                 Text="{Binding Value, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" />
    <TextBlock Grid.Row="2" FontSize="20" Style="{StaticResource etsTempTextStyle}" VerticalAlignment="Top"
                 Text="{Binding Unit, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" />

    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
      <TextBlock  Text="{Binding SettingValue, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}"
                    Style="{StaticResource etsConfigTempTextStyle}" Margin="0,0,3,0" />
      <TextBlock  Text="{Binding Unit, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}"
                    Style="{StaticResource etsConfigTempTextStyle}" FontSize="12" />
    </StackPanel>
  </Grid>
</UserControl>
