<Window x:Class="SystemUpdate.UpdateSelection"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SystemUpdate"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="UpdateSelection" Height="450" Width="800" ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes\Themes.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Background="#191919">
        <Label x:Name="lblUsername" Content="Please select updates" Foreground="White" Height="50" FontSize="25" Margin="276,0,277,400"/>
        <StackPanel Margin="91,44,98,36">
            <ListBox x:Name="listBoxZone" ItemsSource="{Binding TheList}" Height="287" Background="Azure" HorizontalAlignment="Left" Width="611">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Name="grid" Width="597">
                            <CheckBox Name="CheckBoxZone" Tag="{Binding TheValue}" Content="{Binding TheText}" IsChecked="True" Checked="CheckBoxZone_Checked" Unchecked="CheckBoxZone_UnChecked" Margin="0,5,0,0" />
                            <TextBlock Name="VersionZone" HorizontalAlignment="Right" Text="{Binding TheVersion}" Margin="0,5,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <Button Style="{StaticResource ButtonCryterion}" Content="Update" VerticalAlignment="Center" Click="BtnOK_Click" Height="50" FontSize="20" Foreground="White" Margin="321,364,321,36" />
        <Label x:Name="lblVersion" Content="2.0.0.1" FontWeight="Bold" Foreground="White" HorizontalAlignment="Right" Width="150" Height="50" FontSize="25" RenderTransformOrigin="0.833,0.56" Margin="0,400,-48,0"/>

    </Grid>
</Window>