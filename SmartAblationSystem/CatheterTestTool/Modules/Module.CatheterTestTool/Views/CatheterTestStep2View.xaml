<UserControl x:Class="Module.CatheterTestTool.Views.CatheterTestStep2View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Module.CatheterTestTool.Views"
             xmlns:viewModels="clr-namespace:Module.CatheterTestTool.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:CatheterTestMainWindowViewModel}" >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="CatheterTestThemes.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="SystemStatusTextBlockStyle" BasedOn="{StaticResource StepTitleTextBlockStyle}" TargetType="TextBlock">
                <Style.Resources>
                    <Storyboard x:Key="textBlockFlashStoryboard">
                        <DoubleAnimation Storyboard.TargetProperty="(TextBlock.Opacity)" 
                                         From="0" To="1" Duration="0:0:0.5"
                                         RepeatBehavior="Forever" AutoReverse="True"/>
                    </Storyboard>
                </Style.Resources>
                <Style.Triggers>
                    <Trigger Property="Text" Value="ABLATION">
                        <Trigger.EnterActions>
                            <BeginStoryboard Name="AblationFlash" Storyboard="{StaticResource textBlockFlashStoryboard}"/>
                        </Trigger.EnterActions>
                        <Trigger.ExitActions>
                            <StopStoryboard BeginStoryboardName="AblationFlash" />
                        </Trigger.ExitActions>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Style="{StaticResource TestStepBorderStyle}" Margin="0,0,2,2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Step Info -->
            <Grid Grid.Row="0" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,10">
                    <TextBlock Text="Step 2" Foreground="{StaticResource StepTextBrush}" FontWeight="Bold" FontSize="32"/>
                </StackPanel>
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,10">
                    <TextBlock Text="Ablation Run" Style="{StaticResource StepDescriptionTextBlockStyle}" />
                </StackPanel>
                <Grid Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <ProgressBar Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Orientation="Vertical"
                                 Style="{StaticResource VerticalProgressBar}"
                                 Width="15" Height="80" Margin="0,0,10,0" 
                                 HorizontalAlignment="Right" 
                                 Value="{Binding TestStatusProgress.Step2Progress}"/>
                    <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" VerticalAlignment="Center">
                        <TextBlock Text="{Binding TestStatusProgress.Step2Progress, StringFormat={}{0}%}" Style="{StaticResource StepPercentTextBlockStyle}"/>
                        <TextBlock Text="PASS" Style="{StaticResource StepPassTextBlockStyle}" Visibility="Collapsed"/>
                    </StackPanel>
                </Grid>
            </Grid>
            <Grid Grid.Row="1" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width ="Auto" />
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width ="Auto" MinWidth="150"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="STATUS:" Style="{StaticResource TestStatusTitleStyle}" />
                <TextBlock Grid.Column="2"  Text="{Binding SystemStateString}" Style="{StaticResource SystemStatusTextBlockStyle}" 
                           HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Grid Grid.Column="4" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding TestStatusProgress.CountdownTimer}" HorizontalAlignment="Center" FontSize="48" Foreground="Yellow" FontWeight="Bold" Margin="0"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="TIMER" Style="{StaticResource TesterTextTitleStyle}" FontSize="18" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="sec" Style="{StaticResource TesterTextTitleStyle}" Margin="0" HorizontalAlignment="Left"/>
                </Grid>
            </Grid>
            <!-- Sensor Values-->
            <local:SensorParametersUserControl Grid.Row="2" Margin="10, 0, 10, 0"/>
            <local:TemperatureGraphControl Grid.Row="3" DisplayValue="{Binding SensorParameters.Temperature}" DisplayCount="120"
                                           Margin="20,0, 10, 10" ClearGraph="{Binding ResetTemperatureDisplay}"/>   
        </Grid>
    </Border>
</UserControl>
