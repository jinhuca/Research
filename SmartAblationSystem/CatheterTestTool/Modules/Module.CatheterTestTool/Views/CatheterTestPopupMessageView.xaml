<UserControl x:Class="Module.CatheterTestTool.Views.CatheterTestPopupMessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Module.CatheterTestTool.Views"
             xmlns:dialogs="http://prismlibrary.com/"
             xmlns:converters="clr-namespace:Module.CatheterTestTool.Converters"
             dialogs:ViewModelLocator.AutoWireViewModel="True"
             Width="800" Height="400"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="640">
    <dialogs:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="dialogs:Dialog.WindowStartupLocation" Value="CenterScreen"/>
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="WindowStyle" Value="None"/>
            <Setter Property="AllowsTransparency" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Topmost" Value="True"/>
        </Style>
    </dialogs:Dialog.WindowStyle>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="CatheterTestThemes.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Module.Infrastructure;component/Themes/Generic.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Module.Infrastructure;component/Themes/PrismDialog.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:BooleanToStyleConverter x:Key="booleanToStyleConverter"/>
            <Style TargetType="TextBlock" BasedOn="{StaticResource MessageTextBlockStyle}" x:Key="CatheterTestMessageTextStyle">
                <Setter Property="FontSize" Value="22"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Style="{StaticResource MainBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{Binding Title}" Style="{StaticResource StateMessageTitleTextBlockStyle}"/>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="55"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                           Text="{Binding Message}" Style="{StaticResource CatheterTestMessageTextStyle}"/>
                <Grid Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="{Binding OkButtonText}" 
                                Command="{Binding CloseDialogCommand}" 
                                CommandParameter="OK"
                                IsDefault="True">
                            <Button.Style>
                                <MultiBinding Converter="{StaticResource booleanToStyleConverter}">
                                    <Binding Path="IsPassFailMessageDialog" />
                                    <Binding Source="{StaticResource ButtonOnDialogStylePass}"/>
                                    <Binding Source="{StaticResource ButtonOnDialogStyle}"/>
                                </MultiBinding>
                            </Button.Style>
                        </Button>
                        <Button Content="{Binding CancelButtonText}" 
                                Command="{Binding CloseDialogCommand}" 
                                CommandParameter="Cancel"
                                IsCancel="True">
                            <Button.Style>
                                <MultiBinding Converter="{StaticResource booleanToStyleConverter}">
                                    <Binding Path="IsPassFailMessageDialog" />
                                    <Binding Source="{StaticResource ButtonOnDialogStyleFail}"/>
                                    <Binding Source="{StaticResource ButtonOnDialogStyle}"/>
                                </MultiBinding>
                            </Button.Style> 
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
